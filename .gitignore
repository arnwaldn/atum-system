
# === ATUM internal data (sensitive — keep in private repo or local only) ===
# === Auto-generated caches ===
# === Auto-generated skills ===
# === Fichiers backup auto ===
# === Image & media caches ===
# === OS files ===
# === Python cache ===
# === SENSITIVE - NEVER commit ===
# === Schedules perso (non partag�s) ===
# === Session data (exclude logs, keep memory) ===
# ATUM Audit
# Claude Code Config Backup - .gitignore
# Embedded repos & runtime data
# Exclude UUID session directories (subagents, tool-results)
# Only backup essential configuration files
# Scheduler history (SQLite runtime data)
# Track projects/*/memory/ for MEMORY.md but exclude session transcripts
# Transient runtime data
*.backup.*
*.credentials*
*.jsonl
*.pyc
.DS_Store
.credentials.json
Thumbs.db
__pycache__/
audit_store/
backups/
cache/
chrome/
collective-memory/
common-ground/
data/agence-atum/decisions/
data/agence-atum/finances/
data/agence-atum/rgpd/
debug/
desktop.ini
downloads/
file-history/
graph-queue/
hindsight-export/
hindsight-mirror/
hindsight-pending/
history.jsonl
homunculus/
ide/
image-cache/
instincts/
mcp-needs-auth-cache.json
mcp.json
memory/
paste-cache/
plans/
plugins/
projects/**/*.jsonl
projects/**/sessions/
projects/*/[0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f]-*/
schedules/.history/
schedules/appart-aix-search.json
schedules/whatsapp-cloclo-respond.json
security_warnings_state_*.json
session-env/
sessions/
settings.local.json
shell-snapshots/
skills/learned/
statsig/
tasks/
telemetry/
tmp/
todos/
